# Dependencies
node_modules/
mobile/node_modules/

# Native build output — regenerated by `expo prebuild` / `pod install`
mobile/ios/build/
mobile/ios/Pods/
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
DerivedData/
*.xcuserdatad/
*.xcworkspace/xcuserdata/

# Expo
.expo/
mobile/.expo/
dist/
web-build/

# Local backups — large, and not part of the source
backups/

# Environment. NEVER commit real credentials.
.env
.env.local
mobile/.env

# Python
__pycache__/
*.py[cod]
.pytest_cache/
venv/
.venv/

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*

# Fine-tuned DistilBERT weights: 255 MB, above GitHub's 100 MB hard limit.
# The training script (therapist_engine/ml/) reproduces them; the engine
# reports the model as unavailable and fails closed when they are absent.
therapist_engine/ml/detector_bert/bert_model/model.safetensors

# Xcode local state
mobile/ios/DerivedData/

# Local demo/scratch artifacts
*.pdf.png

# Editor/agent tool config
.claude/
