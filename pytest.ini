[pytest]
# Backups keep duplicate copies of test modules; discovering both causes
# import collisions. The backups themselves must not be deleted.
norecursedirs = backups mobile risk_engine/archive .* node_modules
testpaths = therapist_engine/tests
pythonpath = therapist_engine
